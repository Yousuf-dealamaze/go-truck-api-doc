meta {
  name: Reset Password
  type: http
  seq: 5
}

post {
  url: {{baseURL}}/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "resetToken": "dummy-token",
    "newPassword": "NewPassword@123"
  }
}

docs {
  # Reset Password
  
  Resets a user's password using a reset token (usually sent via email/OTP).
  
  ## Payload
  - **resetToken** (string, required): The token received for password reset.
  - **newPassword** (string, required): The new password to set.
}
