meta {
  name: Dashboard Stats
  type: http
  seq: 6
}

get {
  url: {{baseURL}}/users/dashboard
  body: none
  auth: inherit
}

params:query {
  userType: consumer
}

docs {
  # Dashboard Stats
  
  Retrieves user dashboard statistics.
  
  ## Query Params
  - **userType** (string, required): `super_admin`, `admin`, `partner`, `transporter`, `driver`, `transporter_user`, `consumer`, `client`, `client_user`.
}
