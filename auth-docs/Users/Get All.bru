meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/users/all
  body: none
  auth: inherit
}

params:query {
  page: 1
  limit: 10
  search: 
  userType: client
  status: active
  gender: 
  sortBy: createdAt
  sortOrder: desc
}

docs {
  # Get All Users
  
  Retrieves a paginated list of all users. Filter by `userType` (e.g., `client`, `client_user`) and `status` (e.g., `active`, `pending`).
}
