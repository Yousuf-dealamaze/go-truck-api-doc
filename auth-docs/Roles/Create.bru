meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/roles/create
  body: json
  auth: inherit
}

body:json {
  {
    "role_title": "Manager",
    "permissions": {
      "user": {
        "view": true,
        "create": true
      }
    }
  }
}

docs {
  # Create Role
  
  Creates a new role with permissions.
  
  ## Payload
  - **role_title** (string, required)
  - **permissions** (object, required): Map of permissions.
}
