meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/notification-event-master/all
  body: none
  auth: none
}

params:query {
  ~page: 1
  ~limit: 10
}

docs {
  Retrieves all notification events with pagination.
  
  **Query Parameters:**
  * `page` (number, optional): Page number (default: 1)
  * `limit` (number, optional): Items per page (default: 10)
}
