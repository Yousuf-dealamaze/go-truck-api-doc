meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/notification-event-master/create
  body: json
  auth: none
}

body:json {
  {
    "eventCode": "BOOKING_CREATED",
    "eventName": "New Booking Created",
    "module": "BOOKING",
    "category": "Booking Updates",
    "status": "active"
  }
}

docs {
  Creates a new notification event.
  
  **Required Fields:**
  * `eventCode`: Unique code for the event (e.g., 'BOOKING_CREATED')
  * `eventName`: Human-readable name for the event
  * `module`: Module to which the event belongs (e.g., 'BOOKING', 'USER')
  
  **Optional Fields:**
  * `category`: Category name or ID
  * `status`: 'active' or 'inactive'
}
