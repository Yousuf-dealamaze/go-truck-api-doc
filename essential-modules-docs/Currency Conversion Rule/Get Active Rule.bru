meta {
  name: Get Active Rule
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/currency-conversion-rule/active
  body: none
  auth: none
}

params:query {
  date: 2024-06-01
  conversionType: markup
}

docs {
  Retrieves the active conversion rule applicable for a specific date.
  
  **Query Parameters:**
  *   `date` (Date, optional): Date to check (ISO YYYY-MM-DD). Defaults to current date.
  *   `conversionType` (string, optional): 'markup' or 'markdown'. Defaults to 'markup'.
}
