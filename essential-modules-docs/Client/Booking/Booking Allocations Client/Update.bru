meta {
  name: Update
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/booking-allocations-client/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "attemptStatus": "ACCEPTED"
  }
}
