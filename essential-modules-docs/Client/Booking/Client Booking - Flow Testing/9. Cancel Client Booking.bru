meta {
  name: 9. Cancel Client Booking
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/booking-client-master/{{clientBookingId}}
  body: json
  auth: inherit
}

body:json {
  {
    "status": "CANCELLED"
  }
}
