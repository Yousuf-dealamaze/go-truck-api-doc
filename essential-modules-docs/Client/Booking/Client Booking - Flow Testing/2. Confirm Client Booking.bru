meta {
  name: 2. Confirm Client Booking
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/booking-client-master/{{clientBookingId}}
  body: json
  auth: inherit
}

body:json {
  {
    "status": "CONFIRMED"
  }
}
