meta {
  name: Delete (Testing Only)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/billing-ledger-master/:id
  body: none
  auth: inherit
}

params:path {
  id: 
}

docs {
  ### Delete Ledger Entry (Testing Only)
  Performs a soft-delete on the ledger entry.
}
