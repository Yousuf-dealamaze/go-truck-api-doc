meta {
  name: Update
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/billing-invoice-charges/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "amount": 250.00,
    "status": "Approved",
    "description": "Adjusted port handling charges"
  }
}

docs {
  ### Update Invoice Charge (TESTING ONLY)
  Updates an existing invoice additional charge.
  
  **Path Parameters:**
  - `id`: The UUID of the invoice charge.
  
  **Body Fields (All Optional):**
  - `chargesType`: Enum
  - `documentId`: UUID
  - `description`: String
  - `unitRate`: Decimal
  - `quantity`: Integer
  - `amount`: Decimal
  - `status`: Enum
  - `jsonMetadata`: JSONB
  
  **Success Response (200 OK):**
  ```json
  {
    "success": true,
    "code": 200,
    "message": "Successfully updated",
    "data": null
  }
  ```
}
